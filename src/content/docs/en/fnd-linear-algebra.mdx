---
title: "Recap Linear Algebra"
---

import ScalarMult from "../../../components/LinearAlgebra/ScalarMult.astro";
import AddVector from "../../../components/LinearAlgebra/AddVector.astro";
import VectorLength from "../../../components/LinearAlgebra/VectorLength.astro"
import UnitVector from "../../../components/LinearAlgebra/UnitVector.astro"
import DotProduct from "../../../components/LinearAlgebra/DotProduct.astro"

export const components = {
    blockquote: Blockquote,
    a: A,
    pre: Pre,
    pre: InlineCode,
    em: Em,
    h1: H1,
    h2: H2,
    h3: H3,
    h4: H4,
    h5: H5,
    h6: H6,
    hr: Hr,
    img: Img,
    ul: Ul,
    ol: Ol,
    li: Li,
    strong: Strong,
    p: P
}

## Scalar

<Definition
    definition="A scalar is a one-component quantity that is invariant under rotations of the coordinate system."
    source="Scalar - Mathworld Wolfram"
    link="https://mathworld.wolfram.com/Scalar.html"
/>

<Equation
    formula="a"
    description="Scalar representation."
/>

## Vector

<Definition 
    definition="In mathematics, physics, and engineering, a Euclidean vector or simply a vector (sometimes called a geometric vector or spatial vector) is a geometric object that has magnitude (or length) and direction."
    source="Euclidean vector  - Wikipedia"
    link="https://en.wikipedia.org/wiki/Euclidean_vector"
/>

<Equation
    formula="\vx = \begin{bmatrix} x_1 \\ x_2 \\ \vdots \\ x_n \end{bmatrix}"
    description="Vector representation."
/>

Operations on vectors:
- Scalar multiplication
- Addition and Subtraction
- Length
- Normalized vector
- Dot product
- Cross product

### Scalar Multiplication

<Equation
    formula="a \cdot \vv"
    description="Scalar **bold** multiplication. The scalar $a$ multiply the vector $\vv$"
/>

<ScalarMult
    description="original vector"
/>

### Addition and Subtraction

<Equation
    formula="\vc + \vd"
    description="Addition and Subtraction of vectors."
/>

<AddVector
    description="Interaction between vectors addition"
/>

### Length

#### The norm of a vector

<Equation
    formula="\Vert \vx \Vert_2 = \sqrt{x_1^2 + x_2^2 + \cdots + x_n^2}"
    description="On the n-dimensional Euclidean space $\sR^n$ the intuitive notion of length of the vector $\vx = (x_1, x_2, \cdots , x_n)$ is captured by this formula"
/>

The Euclidean norm is also called the $L^{2}$ norm, $\ell^{2}$ norm, 2-norm, or square norm; see $L^{p}$ space.

<VectorLength
    description="Representation of Euclidean norm"
/>

#### The Norm of a Matrix

Just to recap, the triangle inequality says:

<Equation
    formula="\Vert \vx + \vy \Vert \leq \Vert \vx \Vert \Vert \vy \Vert"
    description="Triangle inequality."
/>

This also applies to matrices:

<Equation
    formula="\Vert \mA + \mB \Vert \leq \Vert \mA \Vert \Vert \mB \Vert"
    description="Triangle inequality applied to matrices."
/>

### Normalized Vector

<Equation
    formula="\uvx = \frac{\vx}{\Vert \vx \Vert_2}"
    description="The term *normalized vector* is sometimes used as a synonym for *unit vector*."
/>

<UnitVector
    description="Unit vector representation"
/>

### Dot Product

<Equation
    formula="\va \cdot \vb = \sum_{i=1}^{n} a_{i} b_{i}"
    description="Coordinate definition of the dot product."
/>

<Equation
    formula="\va \cdot \vb = \Vert \va \Vert \cdot \Vert \vb \Vert cos\theta"
    description="Geometric definition of the dot product. Where $\Vert \va \Vert$ and $\Vert \vb \Vert$ are the euclidean norm and $\theta$ is the angle between the vectors"
/>

$$
\vx \cdot \vy = \begin{Vmatrix} \vx \end{Vmatrix} \begin{Vmatrix} \vy \end{Vmatrix} \cos{\theta}
$$

if $\vx \cdot \vy = 0$, $\vx$ and $\vy$ are orthogonal

$$
\cos{\theta} = \frac{\vx \cdot \vy}{\begin{Vmatrix} \vx \end{Vmatrix} \begin{Vmatrix} \vy \end{Vmatrix}}
$$

<DotProduct/>

### Cross Product

<Equation
    formula="\va \times \vb = \Vert \va \Vert \Vert \vb \Vert sin(\theta) \uvn"
    description="where: - $\theta$ is the angle between $\va$ and $\vb$ in the plane containing them $\Vert \va \Vert$ and $\Vert \vb \Vert$ are the magnitudes of vectors $\va$ and $\vb$ and $\uvn$ is a unit vector perpendicular to the plane containing $\va$ and $\vb$, with direction such that the ordered set ($\va$, $\vb$, $\uvn$) is positively-oriented."
/>

## Matrices

Rectangular array of numbers

$$
\mX = \begin{pmatrix}
x_{11} && x_{12} && \cdots && x_{1m} \\
x_{21} && x_{22} && \cdots && x_{2m} \\
\vdots && \vdots && \ddots && \vdots \\
x_{n1} && x_{n2} && \cdots && x_{nm} \\
\end{pmatrix} \in \sR^{n \times m}
$$

note:
- First index refers to row
- Second index refers to column

### Types of Matrices

- Square matrix
- Diagonal matrix
- Identity matrix $\mI$ or $\mI_n$
- Upper and lower triangular matrix
- Symmetric matrix $\mX = \mX^\transpose$
- Skew-symmetric matrix $\mX = -\mX^\transpose$
- Positive (Semi) definite matrix $\va^\transpose \mX \va \geq 0$
- Orthogonal matrix $\mX^\transpose = \mX^{-1}$

### Operations on Matrices

- Matrix-vector multiplication $\mM \vx$
- Matrix-matrix multiplication $\mM_1 \mM_2$
- Inverse $\mM^{-1}$
- Transpose $\mM^\transpose$

## Tensor


