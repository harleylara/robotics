---
const { src, description } = Astro.props;
---

<div class="flex flex-col items-center border-[0.3px] border-dark-100 py-4">
    <img src={src} alt={description}>
    <em class="pt-4">Figure X.X: {description}</em>
</div>
